import { FiltroForoPipe } from './filtro-foro.pipe';

describe('FiltroForoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroForoPipe();
    expect(pipe).toBeTruthy();
  });
});
