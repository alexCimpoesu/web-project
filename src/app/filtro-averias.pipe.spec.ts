import { FiltroAveriasPipe } from './filtro-averias.pipe';

describe('FiltroAveriasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAveriasPipe();
    expect(pipe).toBeTruthy();
  });
});
